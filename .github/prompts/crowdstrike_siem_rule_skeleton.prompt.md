# CrowdStrike SIEM rule skeleton

Given an Objective YAML, produce a placeholder CrowdStrike SIEM rule/query skeleton:
- include objective id reference
- do not invent exact schema if unknown; add TODO placeholders
- include tuning allowlists and test plan

Output YAML.
